apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-additional-config
  namespace: monitoring
  labels:
    app: prometheus
data:
  additional-scrape-configs.yaml: |
    - job_name: 'pokeapi-flask-app'
      static_configs:
        - targets: ['server-service.default.svc.cluster.local:5000']
